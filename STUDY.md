读取模型数据，全是三维空间中的三角形。
对视锥中的点进行mvp变化到标准立方体。
    m: 模型变化，模型在世界空间中平移旋转缩放等。
    v: 视图变化，摄像机移动到原点，看向-z，头顶沿着y轴。物体也要跟随变化。
    p: 投影变化，把视锥体中的点变化成标准立方体。
标准立方体中的点变化到屏幕空间，并划线填色。
着色。
    漫反射。固定的一个值。
    环境光。表面系数，光源强度，与光源的距离，
    高光。光线与物体表面夹角，光源强度，表面系数，与光源的距离。

20251130
做作业3的时候，发现小牛方向有问题，颜色也和示例不一样。发现是之前作业2修正投影矩阵的导致的，为什么呢？修正的矩阵理论上没问题，之后的栅格化深度测试判定大小有些问题，导致显示小牛后面的颜色值。
有一种修改方法，不用动的栅格化深度测试判定，只需要修改投影矩阵：
    const float n = zNear, f = zFar;    //这里就不用取负了。
    const auto tan_rad = tan(eye_fov / 2 * MY_PI / 180);
    const auto t = -zNear * tan_rad, b = -t, l = -t, r = t; //t的计算加个负号。

20251209
不用上述方法，也可以在mvp矩阵乘以一个z轴取反矩阵，然后const float n = -zNear, f = -zFar; 
作业2的描述中说了，框架代码对z值反转，全部取正，越大越远。但是框架代码实际上没有做到了，因此小牛深度值不对，作业2三角形层级关系也反了。
参考：https://zhuanlan.zhihu.com/p/509902950

而且1130日写道“之后的栅格化深度测试判定大小有些问题，导致显示小牛后面的颜色值”，但是我这些天忘了。。。。。。

想到2个问题，1. mvp变化中z中单领出来线性缩放到-1 ~ 1，之后是不是就不用做深度透视矫正？2. 计算重心坐标、透视矫正。

20251210
想到个问题，投影矩阵是又两个矩阵得到的，p2（投影） * p1（正交投影）。p1的结果是齐次的向量，第四位w的值是z，p2乘的结果是这个齐次向量。mvp变化后的结果在除以w可以吗？
在下用式子算了算了，发现是等价的。然后还想到Ax = b，两边同乘实数是可以的。数学没好好学就这样。。。。